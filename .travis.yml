language: python

python:
  - "2.7"
  - "3.3"
  - "3.4"

branches:
  only:
    - master

before_install:
  - pip install -U pip

install:
  - pip install -r requirements.txt
  - pip install requests_mock
  - python setup.py install

script:
  - py.test
